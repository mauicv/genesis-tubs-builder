var primatives = require('../../../primatives/primatives.js')
const fs = require('fs');

const loc = window.location.pathname;

const state = {
  scale: 1,
  center:[0,0],
}

const mutations = {}

const actions = {}

export default {
  state,
  mutations,
  actions
}
